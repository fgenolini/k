plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.5.0'
}

repositories {
    mavenCentral()
}

kotlin {
  //macosX64('native') { // on macOS
  // linuxX64('native') // on Linux
  mingwX64('native') {// on Windows
    binaries {
      executable()
    }
  }
}

dependencies {
    nativeMainImplementation project(':hello')
}

wrapper {
  gradleVersion = '6.9'
  distributionType = 'BIN'
}

